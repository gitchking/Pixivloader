# Legacy Python Scripts

> **Deprecated**: These scripts are legacy code. Please use the main Python backend at `python-backend/` instead.

This directory contains old Python utilities that have been superseded by the main Python backend.

For the current Python backend, see: `../../python-backend/`
